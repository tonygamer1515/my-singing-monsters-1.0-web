<template name="template_removecost">
	
	<hudScale scale="game.hudScale()" />
	
	<size dependentOnElement="element:name()" dependentOnComponent="Text" />
	
	<textComponent name="Text">
		<event name="onInit" params="component, element">
			component("font"):Set(game.getTitleFont())
			component("size"):SetFloat(0.25)
			component("alignment"):SetInt(lua_sys.MenuTextComponent_TEXT_LEFT_ALIGNED)
			component("text"):SetString("-"..game.commaizeNumber(game.objectCost()))
			
			component("red"):SetFloat(1.0)
			component("green"):SetFloat(0.9)
			component("blue"):SetFloat(0.09)
			
			component("layer"):SetString("ContextBar")
		</event>
		<event name="onTick" params="component, element, dt">
			if(manager:isIdle() == true) then
				component("text"):SetString("-"..game.commaizeNumber(game.objectCost()))
			end
		</event>
	</textComponent>
	
</template>